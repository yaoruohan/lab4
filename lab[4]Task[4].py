import math
def subtract(d1,d2):
    res = dict()
    for key in d1:
        if key not in d2:
            res[key] = None
        return res
    words = process.file('book.txt')
    diff = subtract(hist,words)
    print("The words in the book that are not in the word list are: ")
    for word in diff.keys():
      print(word)















